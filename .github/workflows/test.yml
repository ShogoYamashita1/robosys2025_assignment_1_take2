name: test
on: push
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Compile
      run: gcc dice_roll.c -o dice_roll

    - name: ALL test
      run: bash -xv ./test.bash
